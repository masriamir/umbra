"""AppConfig for the todo application."""

from django.apps import AppConfig


class TodoConfig(AppConfig):
    """AppConfig for the todo application."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "todo"
